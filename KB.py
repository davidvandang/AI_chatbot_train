import pandas as pd
from DB import Database

class KnowledgeBase:
    # Start session for database and get the table
    def __init(self, db_name):
        self.db = Database(db_name)

    # Get the table needed for function in KB
    def get_data(self, table_name):

    # Get Answer based on matching questions

    # Webscraping with user information to get the ticket

    # Add rule

    # Remove rule

    # Get all rules



